import React from 'react';
import { SiPorsche } from '@icons-pack/react-simple-icons';

export const PorscheIcon = ({ color = 'currentColor', size = 24, ...props }) => (
  <SiPorsche color={color} size={size} {...props} />
);
