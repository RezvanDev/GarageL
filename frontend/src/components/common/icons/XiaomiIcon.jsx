import React from 'react';
import { SiXiaomi } from '@icons-pack/react-simple-icons';

export const XiaomiIcon = ({ color = 'currentColor', size = 24, ...props }) => (
  <SiXiaomi color={color} size={size} {...props} />
);
