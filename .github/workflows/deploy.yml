name: Deploy Garage Platform

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            # Navigate to project directory (using absolute path for root)
            cd /root/my-garage || cd ~/my-garage
            
            # Discard manual changes on server to avoid git pull conflicts
            git reset --hard
            
            # Pull latest changes from GitHub
            git pull origin main
            
            # Restart containers using docker-compose (v1 compatibility)
            docker-compose up -d --build
